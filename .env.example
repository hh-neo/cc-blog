DATABASE_URL=postgres://postgres:password@localhost:5432/message_board
JWT_SECRET=your-secret-key-change-this-in-production
PORT=3000

# For SQLx compile-time verification (optional)
# If you want to use SQLx compile-time checking, install sqlx-cli:
# cargo install sqlx-cli --no-default-features --features postgres
# Then run: cargo sqlx prepare